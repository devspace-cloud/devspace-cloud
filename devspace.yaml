version: v1beta2
deployments:
  - name: devspace-cloud
    helm:
      chart:
        name: ./chart
      values:
        database:
          password: "MY_PASSWORD"
        ingress:
          domains:
            - my-domain.com
cluster:
  namespace: devspace-cloud
